---
name: swiftui-ui-patterns
description: DEPRECATED — merged into swift-engineering. Do not use.
disable-model-invocation: true
user-invocable: false
---

# SwiftUI UI Patterns — DEPRECATED

This skill was merged into **`swift-engineering`** on 2026-06-03.

`swiftui-ui-patterns` covered building and refactoring SwiftUI UI with component patterns —
navigation, state ownership, layouts, controls, and screen composition. The unique content
(state ownership summary table, sheets guidance, component workflow) is now documented in the
**"SwiftUI View Structure"** mode in `swift-engineering`.

**Use [`swift-engineering`](../../engineering/swift-engineering/SKILL.md) instead.** Trigger phrases
that previously routed here — "build this SwiftUI UI", "component patterns", "navigation setup" —
now route to `swift-engineering`.

This file is retained only as a tombstone. It lives under `skills/deprecated/`,
so `link-skills.sh` skips it and it is never symlinked or auto-discovered.

See `docs/adr/0008-swift-skill-core4-consolidation.md` for the consolidation rationale.
