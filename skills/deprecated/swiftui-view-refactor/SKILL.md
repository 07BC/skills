---
name: swiftui-view-refactor
description: DEPRECATED — merged into swift-engineer. Do not use.
disable-model-invocation: true
user-invocable: false
---

# SwiftUI View Refactor — DEPRECATED

This skill was merged into **`swift-engineer`** on 2026-06-03.

`swiftui-view-refactor` covered refactoring SwiftUI views into stable, testable structure —
splitting large views, tightening data flow, enforcing view member ordering, subview extraction,
and stable view tree rules. These are now documented as the **"SwiftUI View Structure"** mode
in `swift-engineer`.

**Use [`swift-engineer`](../../engineering/swift-engineer/SKILL.md) instead.** Trigger phrases
that previously routed here — "refactor this view", "split this large view", "clean up this
SwiftUI" — now route to `swift-engineer` (rewrite/refactor mode).

This file is retained only as a tombstone. It lives under `skills/deprecated/`,
so `link-skills.sh` skips it and it is never symlinked or auto-discovered.

See `docs/adr/0008-swift-skill-core4-consolidation.md` for the consolidation rationale.
