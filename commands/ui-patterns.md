---
description: Design or refactor a SwiftUI screen using strong default UI patterns.
argument-hint: [screen / feature / file to work on]
---
Use the `swiftui-ui-patterns` skill to design or refactor this SwiftUI UI with strong default patterns: $ARGUMENTS

Follow the skill: pick the right component reference from its `components-index.md`, match local repo conventions, and build with small focused subviews and SwiftUI-native data flow. If no target is given above, ask which screen or feature to work on.
